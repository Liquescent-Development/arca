# Documentation .gitignore
#
# This directory contains both public-facing documentation (kept in git)
# and internal planning/task list documents (ignored).
#
# Public documentation (NOT ignored):
# - ARCHITECTURE.md
# - OVERVIEW.md
# - LIMITATIONS.md
# - DISTRIBUTION.md

# Ignore all markdown and yaml files by default
*.md
*.yaml

# But keep the public-facing documentation
!ARCHITECTURE.md
!OVERVIEW.md
!LIMITATIONS.md
!DISTRIBUTION.md
!VMINIT_BUILD.md

# Per-release artefact records: digests Gas Can's pin verifies, so they are public
!RELEASE-ARTIFACTS-*.md

# Measurements that committed test docstrings cite. A test in git citing a file
# that is not in git leaves its evidence with no durable home, which is how the
# layer-cache record came to live in another repository's ignored scaffolding.
!EVIDENCE-*.md

# NOTE: `!DESIGN-*.md` is not repeated here. PR #61 merged first and added it above
# the evidence block, with the same reasoning; a second negation would be a no-op in
# the one file whose whole purpose is to be read as an inventory.

# The implementation plan for a tracked design. It spans this repository, the
# containerization submodule and gascan, so no single repository's ignored
# scaffolding is the right home for it.
!PLAN-*.md
